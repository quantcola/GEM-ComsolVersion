m_nMaterials    //number of materials
for (unsigned int i = 0; i < m_nMaterials; ++i) { fmplist >> newMaterial.eps; } //all dielectric permittivites
d2msize // number of domains in COMSOL
for (int i = 0; i < d2msize; ++i) {
    domain;     //domain number
    domain2material[domain];}   //number of material which corresponds (the i-th)
